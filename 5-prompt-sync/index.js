const prompt = require('./node_modules/prompt-sync')();

const name = prompt(`Hello there! What's your name? `);
console.log(`hi ${name}. My name is HAL`);





/* 
Challenge: import the print and circle helpers functions from
the 2-create-modules/ folder and make a program to ask the
user to choose a radius and then print out a message with
stats about a circle with that radius!
*/